TODO:

~Workbench Container not saving inventory (just drop items; maybe)
~Fix Workbench Gui (Dat line sux)
~Finish Workbench Gui (may need to move some slots around to make maximum use of the space)
~Setup some way to identify which card is being modified in the Workbench Gui(enchantment effect, or color the box or something)
~Get custom textures for Workbench Buttons
~Fix/actually implement shift-clicking in Workbench Gui
~Setup method in armorTick for detecting the upgrades installed (functions for each upgrade)(Use .getClass() to sort items?)
~Actually texture the Workbench (work on 3d model later)
~Fix the mcmod.info